// Re-export the rendered viewport from the useToast hook so that
// consumers can import `Toast` (the viewport) from the design-system barrel.
export { ToastViewport as default, ToastViewport } from '../../hooks/useToast';
